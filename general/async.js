import { fakeAsyncWork } from '../helpers';

/** 
 * Write a function that returns a Promise that resolves with a string,
 * 'Hello, future me' after some period of time (not immediately, use setTimeout)
 * 
 * HINT: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise
 *       https://www.w3schools.com/jsref/met_win_settimeout.asp
 */
export const helloFutureMe = () => {

};

/**
 * Write a function that handles a promise (fakeAsyncWork)
 * For a successful resolution, return the promised value
 * For a caught error, console.error the error message
 * 
 * NOTE: Be sure to pass workFn to fakeAsyncWork when called
 */
export const handlePromise = (workFn) => {

};

/**
 * Now, do the same, but with async/await syntax
 *
 * HINT: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function
 */
export const handleAsyncAwait = async (workFn) => {

};
